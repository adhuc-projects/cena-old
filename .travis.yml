language: java
sudo: required
dist: trusty

cache:
  directories:
  - $HOME/.m2

jobs:
  include:
    - stage: commit stage
      script:
      - make build

notifications:
  email:
    on_success: change
    on_failure: always